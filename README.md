Before install download Vim Plug after that install PlugInstall. After the Plug installation download node js and npm and go to ~/.local/share/nvim/plugged/coc.nvim
and install yarn (npm install -g yarn) yarn install && yarn build that nvim is ready.

:CocInstall coc-tsserver
:CocInstall coc-python
:CocInstall coc-go
:CocInstall coc-java
